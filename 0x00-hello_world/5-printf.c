#include <stdio.h>
/**
 * main - A programe that prints in line using the printf function
 * Return: 0 (pass)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
